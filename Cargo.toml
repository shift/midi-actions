[package]
name = "midi-hub"
version = "0.1.0"
edition = "2021"

[dependencies]
midir = "0.9"
evdev = "0.12"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
clap = { version = "4.4", features = ["derive"] } # For CLI flags
anyhow = "1.0"
